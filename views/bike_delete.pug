block content

  h1= title
  
  p #[strong Do you really want to delete this Bike?]
  
  div(style="margin-left:20px")

    p #[strong ID]: #{bike._id}
  
    p #[strong Model:] 
      a(href=bike.url) #{bike.model}

    p #[strong Size]: #{bike.size}



  form(method='POST' action='')
    div.form-group
      input#id.form-control(type='hidden',name='id', required='true', value=bike._id )

    button.btn.btn-primary(type='submit') Delete