extends layout

block content
  h1= title

  ul
    each bike in bike_list
      li
        a(href=bike.url) #{bike.model} 
        | #{bike.manufacturer.name} (#{bike.size})
        

    else
      li There are no bikes.
