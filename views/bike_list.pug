extends layout

block content
  h1= title

  ul
    each bike in bike_list
      li
        a(href=bike.url) #{bike.title}
        |  (#{bike.manufacturer.name})

    else
      li There are no bikes.
